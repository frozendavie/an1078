<!-- liblinks.xml

     Code Red Technologies "Smart update wizard" script file
     When executed on a particular application project, will
     add appropriate links to the specified library project.

     Note that this script assumes that the application project
     contains the standard 'Debug' and 'Release' build 
     configurations.
-->

<project name="" update="true">
	<setting id="all.compiler.inc">
		<value>${workspace_loc:/CMSIS_DSPLIB_CM4/inc}</value>
        </setting>
	<setting id="linker.libs">
		<value>CMSIS_DSPLIB_CM4</value>
	</setting>
	<setting id="linker.paths">
		<value>${workspace_loc:/CMSIS_DSPLIB_CM4/lib}</value>
	</setting>
	<requires msg="Library project `CMSIS_DSPLIB_CM4` not found">
		<value>CMSIS_DSPLIB_CM4</value>
	</requires>
</project>

